.DS_Store
node_modules/
.vercel
*.log

# SDD scratch: ledger, briefs, review packages
.superpowers/

# regenerated chart data, pasted into index.html by hand
tools/out/
.playwright-mcp/

# Local real-money trading platform.  It contains proprietary strategy code,
# broker credentials, access tokens and multi-gigabyte tick recordings.
/RVWAP Live/
/RVWAP Pristine BT Engines/

# Obsidian vault documenting the above.  Same reason: it records the exact
# strategy rules, constants, execution logic and incident history.
/QuantNifty Vault/

# Per-client capital scaling plans.  This GitHub repo is public and these name
# real individuals alongside their capital figures, so they stay local.  They
# are already kept off the deployed site by .vercelignore.
/Website/docs/clients/
/Website/output/pdf/quantnifty-*-scaling.pdf

# Strategy documentation: exact sleeve parameters, per-year P&L and drawdowns.
# Same reason as the vault above -- this repo is public.
/Documentation/

# Generator for the forward-test page. It imports the research engine modules by
# name and those names describe the strategy, so it stays out of a public repo.
# The generated Website/forward-test/index.html is committed; this is not.
/Website/forward-test/build/
